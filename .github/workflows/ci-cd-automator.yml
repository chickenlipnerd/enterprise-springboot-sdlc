name: OpenShift Deploy Placeholder

on:
  workflow_dispatch:

jobs:
  placeholder:
    runs-on: ubuntu-latest

    steps:
      - name: Explain placeholder status
        run: |
          echo "This workflow is intentionally manual."
          echo "It is a safe placeholder for future OpenShift/CD automation."
          echo "Before enabling real deployment, add manifests, registry publishing, and environment secrets."
